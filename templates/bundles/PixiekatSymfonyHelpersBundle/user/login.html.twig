{% extends '@HMFPSearchTool/search/layout.html.twig' %}

{% block content %}
  {% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
  {% endif %}

  <div class="container">
    <div class="row">
      <div class="col">
        <h1>Login</h1>
        <p>Login to your account</p>

        <div class="login-form" data-turbo="false">
            {{ form(form) }}
        </div>
      </div>
    </div>
  </div>
{% endblock %}
