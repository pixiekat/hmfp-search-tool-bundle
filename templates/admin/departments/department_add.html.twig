{% extends '@HMFPSearchTool/admin/layout.html.twig' %}
{% block admin_page_title %}Add Department{% endblock %}

{% block content %}
  <div class="actions">
    <a href="{{ path('hmfp_search_tool_admin_departments_list') }}" class="button button--secondary">&larr; Back to Departments</a>
  </div>

  <div data-turbo="false">
    {{ form_start(form) }}
      {{ form_widget(form) }}
      <button type="submit">Add Department</button>
    {{ form_end(form) }}
  </div>
{% endblock %}
