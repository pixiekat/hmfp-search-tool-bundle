{% extends '@HMFPSearchTool/admin/layout.html.twig' %}
{% block admin_page_title %}Users{% endblock %}

{% block content %}
  <div class="actions">
    <a href="{{ path('hmfp_search_tool_admin_departments_add') }}" class="button">Add Department</a>
  </div>

  <div class="table-wrap">
    <table>
      <caption>{{ total }} department{{ total == 1 ? '' : 's' }} · page {{ page }} of {{ pages }}</caption>
      <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col"># of Physicians</th>
          <th scope="col"><span class="visually-hidden">Actions</span></th>
        </tr>
      </thead>
      <tbody>
        {% for department in departments|default([]) %}
          <tr>
            <th scope="row">{{ department.getName }}</th>
            <td>{{ department.getPhysicians|length }}</td>
            <td>
              {# Accessible names say WHICH department — a column of identical "Edit"
                 and "Delete" controls is unusable in a screen reader's list. #}
              <a href="{{ path('hmfp_search_tool_admin_department_edit', {'id': department.id}) }}" class="button button--secondary">
                Edit<span class="visually-hidden"> {{ department.getName }}</span>
              </a>
              <form method="post" action="{{ path('hmfp_search_tool_admin_department_delete', {'id': department.id}) }}" class="inline"
                    onsubmit="return confirm('Delete {{ department.getName }}? This cannot be undone.');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete-department-' ~ department.id) }}">
                <button type="submit" class="button--danger">
                  Delete<span class="visually-hidden"> {{ department.getName }}</span>
                </button>
              </form>
            </td>
          </tr>
        {% else %}
          <tr><td colspan="3">No departments found.</td></tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  {% if pages > 1 %}
    <nav class="pagination" aria-label="Departments pages">
      {% if page > 1 %}
        <a href="{{ path('hmfp_search_tool_admin_departments_list', {page: page - 1}) }}" rel="prev">← Previous</a>
      {% else %}
        <span class="muted" aria-hidden="true">← Previous</span>
      {% endif %}
      <span>Page {{ page }} of {{ pages }}</span>
      {% if page < pages %}
        <a href="{{ path('hmfp_search_tool_admin_departments_list', {page: page + 1}) }}" rel="next">Next →</a>
      {% else %}
        <span class="muted" aria-hidden="true">Next →</span>
      {% endif %}
    </nav>
  {% endif %}
{% endblock %}
